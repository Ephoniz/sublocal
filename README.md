# sublocal

Local-only subtitle translation (and later transcription) with small models. No cloud APIs.

Phase 1: translate existing `.srt` / `.vtt` / `.ass` files, keep timestamps.

See upcoming commits for the CLI.