[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sublocal"
version = "0.4.0"
description = "Local-only subtitle translation and transcription. No cloud APIs."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Maruan Aguerdouh" }]
keywords = ["subtitles", "translation", "transcription", "nllb", "whisper", "ctranslate2", "offline"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Multimedia :: Video",
]
dependencies = [
  "av>=11.0.0",
  "ctranslate2>=4.0.0",
  "faster-whisper>=1.0.0",
  "huggingface-hub>=0.23.0",
  "langdetect>=1.0.9",
  "regex>=2025.10.22",
  "sentencepiece>=0.2.0",
  "stable-ts-whisperless>=2.18.0",
  "transformers>=4.40.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
lid = ["lingua-language-detector==1.4.2"]

[project.scripts]
sublocal = "sublocal.cli:main"

[project.urls]
Homepage = "https://github.com/Ephoniz/sublocal"

[tool.hatch.build.targets.wheel]
packages = ["sublocal"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
